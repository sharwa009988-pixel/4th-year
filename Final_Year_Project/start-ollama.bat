@echo off
echo ========================================
echo Grok (xAI) is used instead of a local Ollama server
echo ========================================
echo.
echo No local server to start. Ensure GROK_API_KEY is set in your environment
echo and then start the backend: cd backend && mvn spring-boot:run
echo.
pause
